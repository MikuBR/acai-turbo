# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
release
*.local
vite.log

# Dados pessoais do licenciante (NUNCA commitados)
# BUILD_LICENSE_OWNER / BUILD_LICENSE_DOCUMENT ficam só aqui
.env.local

# Build assets (ícones/instalador) — gerados localmente, não versionar
# Os arquivos podem conter dados pessoais preenchidos nos templates (LICENSE/EULA)
# build/  <-- REMOVIDO: necessário para electron-builder no CI

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.agents/
.cline/
.clinerules/
.opencode/

# Local agent instruction files (never commit — stays on this machine only)
AGENTS.md

# SQLite runtime files (WAL mode cria arquivos auxiliares)
*.db-shm
*.db-wal

# Test coverage
coverage/