# Configuração para compilar native modules contra Electron, não Node.js
# Isso evita o erro "NODE_MODULE_VERSION mismatch" ao rodar npm install/rebuild

runtime=electron
target_arch=x64
disturl=https://electronjs.org/headers

# Cache para builds mais rápidos
cache-min=9999999