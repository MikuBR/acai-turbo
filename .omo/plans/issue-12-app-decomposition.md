# Plano — issue #12: Decomposição do App.jsx

**Issue**: https://github.com/MikuBR/acai-turbo/issues/12
**Status**: approved (awaiting `/start-work`)
**Intent**: clear · **Review required**: false
**Decisões do dono** (confirmadas):
1. Rotas reais + URLs (cada antigo modal vira rota com path; UX muda — botão voltar funciona, sem botão X).
2. Guard de auth via `loader` que `throw redirect("/login")` (idiomático React Router v7).
3. Auth migra para `useAuthStore` existente (`src/store/authStore.ts`); demais `useState` ficam locais às features.

## Contexto verificado (executor leia antes de começar)
- `src/App.jsx` = 926 linhas, ~35 `useState`, ~20 handlers. Renderização condicional via um único objeto `modals: { newTable, settings, checkout, reports, login, changePassword }` (App.jsx:41). Listener push iFood main→renderer registrado em App.jsx:418-472 (`ipc.on('ifood:new-order'|'ifood:order-cancelled')`).
- `src/main.jsx` monta `<App/>` sob `<StrictMode><ErrorBoundary>` (main.jsx:1-13).
- `src/store/authStore.ts` JÁ EXISTE: `useAuthStore({ currentUser, authToken, login(user,token), logout() })` — **não usado pelo App.jsx** (auth é `useState` local em App.jsx:47-53).
- `src/store/useStore.ts` (Zustand): tables/catalog/activeTableId/addTable/addItem/removeItem/checkout — **manter como está** (não migrar).
- `src/services/ipc.js` exporta `getIPC()` (já existe). App.jsx tem uma cópia inline idêntica em App.jsx:56-71 — usar a importação de `src/services/ipc.js`, eliminar a duplicata.
- `src/components/templates/{MainLayout,ModalLayout}.jsx` existem como stubs (1-3 linhas). Preferível: criar `src/layouts/AppLayout.jsx` novo e ignorar estes stubs (não os force a servir).
- `src/features/{auth,pdv,reports,settings}/` existem porém **vazios** — preencher.
- `src/layouts/`, `src/router/` vazios — preencher.
- `react-router-dom@^7.18.2` instalado, não usado. API v7 (data mode): `createBrowserRouter(routes)`, `<RouterProvider router={...}/>`, `loader`, `redirect`, `useNavigate`, `Navigate`, `Outlet`, `useLocation`.
- **Testes atuais** (`src/tests/`): cobrem atoms, `useStore`, db, validate, logger — `setup.js` só importa `@testing-library/jest-dom`. **Zero testes de App.jsx / modais / rotas.**
- Vitest config: `src/tests/**/*.{test,spec}.{js,jsx}`, jsdom, globals on. ESLint flat: `eslint.config.js` cobre `src/` (JSX) + CJS do database/main/preload. `npm test`, `npm run lint`, `npm run typecheck` (tsc --noEmit).
- **Não tocar**: `main.cjs`, `preload.js`, `database/**`, `validate.cjs`, canais IPC, `vite.config.js`, `vitest.config.js`, `package.json` (deps já incluem react-router-dom).

## Must-NOT-Have (guardrails — executor NÃO faça)
- NÃO migrar os `useState` de pdv/settings/reports para stores Zustand globais (apenas auth usa `useAuthStore`).
- NÃO editar `main.cjs` / `preload.js` / `database/db.cjs` / `database/validate.cjs` / canais IPC.
- NÃO usar `loader`/data-loaders para dados de negócio (catálogo, mesas, settings) — apenas o auth guard usa `loader`.
- NÃO introduzir `React.lazy` / `Suspense` (não pedido na issue).
- NÃO alterar o `package.json` (deps já presentes) nem configs de build/test/lint.
- NÃO remover gradualmente a funcionalidade — jede mudança preserva 100% do comportamento anterior.

## Fases e dependências
Fase 1 → Fase 2 → Fase 3 → Fase 4 → Fase 5 (estritamente sequenciais; cada fase libera a próxima; QA verde entre fases).

---

## Todos

- [x] 1. Migrar estado de auth do App.jsx para o useAuthStore existente
  - **Refs**: `src/App.jsx` (linhas 47-53: `currentUser`,`authToken`,`loginForm`,`loginError`,`isLoggingIn`; linhas 474-518: `handleLogin`,`handleLogout`; linhas 520-549: `handleChangePassword`; linhas 551-554: `useEffect` que limpa `localStorage.authToken`; linhas 556-576: `hasPermission`,`runWithAuth`); `src/store/authStore.ts` (já tem `currentUser`,`authToken`,`login`,`logout`); `src/tests/store.test.js` (padrão de teste Zustand).
  - **Acceptance**: `useAuthStore` é a única fonte de `currentUser`/`authToken` em `App.jsx`. `handleLogin` chama `useAuthStore.getState().login(user,token)` em vez de `setCurrentUser`/`setAuthToken`. `handleLogout` chama `useAuthStore.getState().logout()`. `localStorage.authToken` continua sendo lido/escrito como hoje (para `auth:verify-session` futuro). `hasPermission`/`runWithAuth`/`runWithManagerAuth`/`isAuthValid`/`authTime`/`showPassModal` permanecem (manter auth de manager via `auth:verify-password`). Nenhuma mudança visual.
  - **QA**: `npm run lint && npm run typecheck && npm test` verdes. Criar `src/tests/authStore.test.js`: testa `login` popula `currentUser`+`authToken`, `logout` zera, e `getState()` independe de React. Smoke `npm run dev`: login com usuário válido entra na tela; logout volta pra tela de login. Evidência: `/.omo/evidence/phase1-auth-store.png` (screenshot do Electron na tela pós-login) + output de `npm test src/tests/authStore.test.js`.

- [x] 2. Criar o router v7 com createBrowserRouter e o loader-guard de auth em src/router/index.jsx
  - **Refs**: docs React Router v7 (`await context7_query-docs('/remix-run/react-router','createBrowserRouter RouterProvider loader redirect')`); `src/main.jsx` (ponto de montagem atual); `src/App.jsx` (toda a lógica que será a rota `/pdv` na Fase 1); `src/store/authStore.ts` (lê `authToken`/`currentUser` no loader); canal IPC `auth:verify-session` (preload.js:28, main.cjs:988-1001).
  - **Acceptance**: `src/router/index.jsx` exporta `router` = `createBrowserRouter([{ path:'/login', Component: LoginScreen }, { path:'/', Component: RootLayout, loader: authGuard, children:[{ index:true, element:<Navigate to='/pdv'> }, { path:'pdv', element:<PdvScreen> }] }] )`. `authGuard({request})` lê `useAuthStore.getState().authToken` (ou `localStorage.authToken`), se ausente `throw redirect('/login')`. **Fase 1 deixa `LoginScreen` e `PdvScreen` como wrappers finos** que importam/exportam de `App.jsx` (ninho provisório) — a decomposição real vem nas Fases 3-5. `LoginScreen` renderiza `<LoginModal>` existente como página cheia (não modal); `PdvScreen` renderiza o `<App>` atual (ainda monolítico, sem o `LoginModal`). `src/main.jsx` troca `<App/>` por `<RouterProvider router={router}/>` dentro de `<StrictMode><ErrorBoundary>`.
  - **QA**: `npm test` verdes. Criar `src/tests/router.test.jsx`: renderiza `<RouterProvider router={testRouter}/>` com um router de teste donde `authToken` está ausente → `screen` contém o `<LoginScreen>` (assert via `data-testid="login-screen"`). Com token mockado (Zustand `useAuthStore.setState({authToken:'x',currentUser:{...}})`) → `screen` contém `PdvScreen` (`data-testid="pdv-screen"`). `npm run dev`: app abre em `/login`; digitar credenciais válidas navega para `/pdv`; URL barra mostra `/pdv`; recarregar em `/pdv` sem sessão volta para `/login`. Evidência: `/.omo/evidence/phase2-router.png` + log `npm test src/tests/router.test.jsx`.

- [x] 3. Criar src/layouts/AppLayout.jsx com Sidebar + Cart + listeners iFood + bootstrap, e torná-lo o layout das rotas autenticadas
  - **Refs**: `src/App.jsx` (716-776 JSX do OrderSidebar + área central + CartPanel; 411-472 listeners iFood + useEffect mount com `syncDB`/`loadPrinterConfig`/`loadIfoodConfig`; 126-140 `playBeep`; 719-730 propsOrdenSidebar); `src/components/organisms/OrderSidebar.jsx`, `CartPanel.jsx` (preservar imports); `src/store/useStore.ts` (`addTable`,`addItemToActiveTable`); `src/services/ipc.js` (`getIPC`).
  - **Acceptance**: `src/layouts/AppLayout.jsx` renderiza `<div flex h-screen>` com `<OrderSidebar>` (esquerda), `<Outlet/>` (centro, o conteúdo da rota filha) e `<CartPanel>` (direita) + `<ToastContainer/>` + `<LoadingOverlay/>`. Extrai 3 hooks de efeito: `useBootstrap()` (chama `syncDB`+`loadPrinterConfig`+`loadIfoodConfig` no mount), `useIfoodEvents()` (registra e desregistra `ifood:new-order`/`ifood:order-cancelled` via getIPC). `OrderSidebar.onNewTable`/`onOpenReports`/`onOpenSettings` trocam `setModals` por `useNavigate('/pdv/new-table'|'/reports'|'/settings')`. `onLogout` chama o handler da Fase 1 e `navigate('/login')`. Atualiza `src/router/index.jsx`: rota `/` usa `Component: AppLayout` (era `RootLayout`) com `loader: authGuard` e `children` com `<Outlet>`. `PdvScreen` agora contém SÓ a área central (pesquisa + grid de produtos + builders) — sidebar/cart vivem no `AppLayout`.
  - **QA**: `npm test`. Criar `src/tests/AppLayout.test.jsx`: renderiza `<AppLayout>` com `<memoryRouter>` e children mock → contem sidebar (`data-testid="order-sidebar"`), cart (`data-testid="cart-panel"`) e o filho. Smoke `npm run dev`: sidebar e cart visíveis em `/pdv`, `/settings` (quando existir) e `/reports`; botões da sidebar navegam; fechar app e reabrir — sem listener iFood duplicado. Evidência: `/.omo/evidence/phase3-applayout.png`.

- [x] 4. Criar src/router/routes.jsx com a configuração completa de rotas (estrutura final alvo), com placeholders apontando para as features ainda-vazias
  - **Refs**: `src/router/index.jsx` (criado na tarefa 2); `src/features/{auth,pdv,reports,settings}/` (vazios — placeholder `export default function(){return null}` temporário por feature é permitido nesta tarefa, mas substituido nas Fases 3-5); `src/components/organisms/{SettingsModal,ReportsModal,CheckoutModal,NewTableModal,AcaiBuilderModal,QuickBuilderModal,PasswordModal,ManagerAuthModal,LoginModal}.jsx`.
  - **Acceptance**: `routes.jsx` define o array de rotas completo: `/login` → `auth/LoginScreen`; `/change-password` → `auth/ChangePasswordScreen`; `/` (layout `AppLayout`, loader `authGuard`) children: `index → Navigate('/pdv')`, `/pdv` → `pdv/PdvScreen`, `/pdv/builder/acai` → `pdv/AcaiBuilderScreen`, `/pdv/builder/quick` → `pdv/QuickBuilderScreen`, `/pdv/new-table` → `pdv/NewTableScreen`, `/checkout` → `pdv/CheckoutScreen`, `/settings` → `settings/SettingsScreen`, `/reports` → `reports/ReportsScreen`. Cada feature exportando um placeholder `() => null` por enquanto (preenchido nas Fases 3-5). `src/router/index.jsx` importa `routes` de `routes.jsx` e `createBrowserRouter(routes)`. **Nada quebra**: os placeholders só ativam quando navegados, e a navegação via sidebar usa `/pdv`+`/settings`+`/reports` que ainda apontam para o `App.jsx` monolítico em PdvScreen até a Fase 3.
  - **QA**: `npm test`. `src/tests/router.test.jsx` estendido: para cada path no array, `router.routes` contém aquele path. Smoke `npm run dev`: navegar manualmente para cada URL (placeholder renderiza vazio sem crash). Evidência: `/.omo/evidence/phase4-routes-map.txt` (`JSON.stringify(router.routes, null, 2)` ou similar).

- [x] 5. Extrair src/features/settings/SettingsScreen.jsx: migrar todos os estados/handlers de settings do App.jsx, e fazer /settings apontar para ela
  - **Refs**: `src/App.jsx` (74-124: `categories`,`newCatName`,`newProd`,`newPromo`,`settingsTab`,`users`,`newUser`,`inventory`,`selectedInventoryItem`,`inventoryMovements`,`inventoryForm`,`financialAccounts`,`financialForm`,`financialFilter`,`clients`,`clientForm`,`selectedClientOrders`,`pwdForm`,`printerConfig`,`ifoodConfig`,`ifoodUnreadCount`,`ifoodConnectionStatus`,`isTestingIfood`; 160-360: `loadUsers`,`loadInventory`,`loadInventoryMovements`,`loadFinancialAccounts`,`loadClients`,`loadClientOrders`,`loadPrinterConfig`,`savePrinterConfig`,`loadIfoodConfig`,`saveIfoodConfig`,`handleTestIfoodConnection`; 828-881: render `<SettingsModal>` com ~40 props); `src/components/organisms/SettingsModal.jsx`.
  - **Acceptance**: `SettingsScreen.jsx` move todos esses `useState` e handlers como estado LOCAIS do componente (não para store). Renderiza `<SettingsModal>` passando as mesmas props — mas `onClose={() => navigate('/pdv')}` em vez de `setModals`. O `useEffect` de mount chama os loaders relevantes (`loadUsers`/`loadInventory`/etc.) via `useEffect` dentro de `SettingsScreen`. Remove do `App.jsx` todos esses estados/handlers e o bloco `{modals.settings && <SettingsModal...>}`. Atualiza `routes.jsx`: `/settings` → `settings/SettingsScreen` (não mais placeholder). `OrderSidebar.onOpenSettings` (em `AppLayout`) continua `navigate('/settings')`. Comportamento idêntico.
  - **QA**: `npm run lint && npm run typecheck && npm test`. Criar `src/tests/SettingsScreen.test.jsx`: renderiza `<SettingsScreen>` dentro de `<MemoryRouter>` com `getIPC` mockado (retorna `{invoke: async()=>({success:true,data:[]})}`) → contém `<SettingsModal>` (`data-testid="settings-modal"`); `onClose` (fechar via navigate) limpa. Smoke `npm run dev`: abrir settings via sidebar → mesma UI; fechar volta para `/pdv`; cada aba (produtos/estoque/financeiro/clientes/iFood/impressoras) carrega dados sem erro. Evidência: `/.omo/evidence/phase5-settings.png` (settings aberto) + log de testes.

- [x] 6. Extrair src/features/reports/ReportsScreen.jsx e src/features/pdv/CheckoutScreen.jsx; /reports e /checkout apontam para elas
  - **Refs**: `src/App.jsx` (84-89: `reportData`,`ordersHistory`,`cashMove`,`reportPeriod`,`advancedReportData`,`financialSummary`; 92: `selectedPromotion`; 230-277: `loadReports`,`loadAdvancedReport`,`loadFinancialSummary`,`calculateDiscount`; 677-713: `handleFinalize`; 792-808: `<ReportsModal>`; 883-892: `<CheckoutModal>`); `src/components/organisms/{ReportsModal,CheckoutModal}.jsx`; `src/layouts/AppLayout.jsx` (`CartPanel.onCheckout` → `navigate('/checkout')`).
  - **Acceptance**: `ReportsScreen.jsx` move estados/handlers de relatório para estado local; renderiza `<ReportsModal>` com `onClose={()=>navigate('/pdv')}`; mount chama `loadReports`. `CheckoutScreen.jsx` lê `activeTable` do `useStore`, `promotions` via store local (ou recebe do PdvScreen via state do router), renderiza `<CheckoutModal>`; `handleFinalize` permanece idêntico, mas em sucesso chama `checkoutActiveTable()` + `navigate('/pdv')`. Remove do `App.jsx` os estados/handlers de relatório/checkout e os blocos JSX `{modals.reports...}`+`{modals.checkout...}`. Atualiza `routes.jsx`: `/reports` → `reports/ReportsScreen`, `/checkout` → `pdv/CheckoutScreen`. `CartPanel.onCheckout` em `AppLayout` → `navigate('/checkout')`. `OrderSidebar.onOpenReports` → `navigate('/reports')`.
  - **QA**: `npm test`. Criar `src/tests/ReportsScreen.test.jsx` + `src/tests/CheckoutScreen.test.jsx` (render com mocks IPC; `activeTable` populado no `useStore`; `handleFinalize` chamado → `orders:save` invocado uma vez; navega para `/pdv`). `npm run dev`: abrir reports → UI igual; checkout finaliza e volta; relatório diário carrega. Evidência: `/.omo/evidence/phase6-reports-checkout.png`.

- [x] 7. Extrair src/features/auth/{LoginScreen,ChangePasswordScreen,ManagerAuthScreen}.jsx; /login, /change-password apontam para elas; ManagerAuth como overlay preservando o padrão atual
  - **Refs**: `src/App.jsx` (474-507: `handleLogin`; 520-549: `handleChangePassword`; 599-606: `isAuthValid`,`runWithManagerAuth`; 894-917: `<LoginModal>`,`<PasswordModal>`,`<ManagerAuthModal>`); `src/components/organisms/{LoginModal,PasswordModal,ManagerAuthModal}.jsx`; `src/store/authStore.ts`; `src/router/index.jsx` (rota `/login` pública, `/change-password` protegida).
  - **Acceptance**: `LoginScreen.jsx` renderiza `<LoginModal>` como full-page (estende CSS para ocupar `h-screen`), `handleLogin` usa `useAuthStore.getState().login()` + `localStorage.setItem('authToken', token)`; em sucesso `navigate('/pdv')` (ou `must_change_password` → `navigate('/change-password')`). `ChangePasswordScreen.jsx` renderiza `<PasswordModal>` como página; sucesso → `navigate('/pdv')`. `ManagerAuthScreen.jsx` mantém o padrão de **overlay/modal** (não rota fullPath) porque `runWithManagerAuth` é um gate síncrono inline (`showPassModal` com `onResult` callback) — para preservar o comportamento, manter `showPassModal` como estado local no `AppLayout` e um `<ManagerAuthModal>` renderizado lá (não vira rota); documentar esta exceção. O `useEffect` que limpa `localStorage.authToken` no mount (App.jsx:552-554) move-se para `AppLayout` (ou para um hook `useAuthBootstrap`). Atualiza `routes.jsx`: `/login` → `auth/LoginScreen`, `/change-password` → `auth/ChangePasswordScreen`. Remove do `App.jsx` os `LoginModal`/`PasswordModal` e seus estados.
  - **QA**: `npm test`. Criar `src/tests/LoginScreen.test.jsx`: render `<LoginScreen>` dentro `<MemoryRouter>`, mock `getIPC.invoke('auth:login')` → sucesso → `useAuthStore.getState().currentUser` populado e `navigate` chamado para `/pdv`. Smoke `npm run dev`: tela inicial é `/login`; login OK → `/pdv`; logout → `/login`; senha expirada → `/change-password`; fluxo manager-auth (botão settings) ainda pede senha do gerente. Evidência: `/.omo/evidence/phase7-auth.png`.

- [x] 8. Extrair src/features/pdv/{PdvScreen,AcaiBuilderScreen,QuickBuilderScreen,NewTableScreen}.jsx e demolir App.jsx
  - **Refs**: `src/App.jsx` (39-44: `searchTerm`,`selectedCategory`,`builder`,`simpleBuilder`; 78-81: `tableType`,`newTableName`,`delivForm`; 578-597: `safeCatalog`,`safeTables`,`activeTable`,`categoriesMenu`,`deferredSearchTerm`,`filteredProducts`,`acaiBases`,`availableAddons` (memos); 608-675: `handleAddTable`,`handleItemSelect`,`toggleRemoval`,`updateExtraInBuilder`,`confirmFullBuild`,`confirmSimpleBuild`; 716-767: JSX da área central + grid); `src/store/useStore.ts` (`catalog`,`tables`,`activeTableId`,`addTable`,`addItemToActiveTable`); `src/components/organisms/{AcaiBuilderModal,QuickBuilderModal,NewTableModal}.jsx`; `src/components/atoms/ProductCard.jsx`; `src/router/routes.jsx`.
  - **Acceptance**: `PdvScreen.jsx` contém a área central (pesquisa, categorias, grid de produtos, botão "Montagem"); estados `searchTerm`/`selectedCategory` locais; lê catalog do `useStore`. Acao "Montagem" → `navigate('/pdv/builder/acai')`; `handleItemSelect` → `navigate(item.category==='COPOS DE AÇAÍ' ? '/pdv/builder/acai' : '/pdv/builder/quick', {state:{product}})`. `AcaiBuilderScreen.jsx`/`QuickBuilderScreen.jsx` recebem `product` via `useLocation().state`, mantêm `builder`/`simpleBuilder` como estado local, em `confirmFullBuild`/`confirmSimpleBuild` adicionam ao `useStore` e `navigate('/pdv')`. `NewTableScreen.jsx` mantém `tableType`/`newTableName`/`delivForm`; `handleAddTable` chama `useStore.addTable` e `navigate('/pdv')`. Todos os builders e NewTableModal deixam de ser modais e passam a ser **rotas full-page** (padding/scroll ajustados para Ana tela cheia). **Demolir `src/App.jsx`**: após migrar tudo, `App.jsx` ou é excluído (com `main.jsx` importando o router) ou reduz-se a um `export { default } from './router'` de uma linha — preferir excluir e atualizar `main.jsx` para `import router from './router'` + `<RouterProvider router={router}/>`. Atualiza `routes.jsx` (`/pdv`, `/pdv/builder/acai`, `/pdv/builder/quick`, `/pdv/new-table` apontam para as features reais). Rodar `grep -rn "from.*App.jsx\\|from.*\\.App'" src/` → zero referências.
  - **QA**: `npm run lint && npm run typecheck && npm test`. Criar `src/tests/PdvScreen.test.jsx`: render `<PdvScreen>` com `useStore` populado → `ProductCard` aparece para cada produto; clicar em "Montagem" → `navigate('/pdv/builder/acai')`. Smoke `npm run dev`: PDV completo — selecionar produto abre builder; confirmar adiciona ao carrinho; nova comanda funciona; delivery com taxa funciona; fluxo iFood (listener) ainda cria mesa delivery; checkout; recarregar em `/pdv` com sessão mantém. Evidência: `/.omo/evidence/phase8-pdv.png` + `grep` output no `/.omo/evidence/phase8-no-app-refs.txt`.

## Final verification wave

- [x] F1. Executar `npm run lint && npm run typecheck && npm test` do zero e confirmar tudo verde; anexar o output em `/.omo/evidence/final-lint-typecheck-test.txt`. Critério: zero erros, zero warnings novos, todos os testes (inclusive os novos: authStore, router, AppLayout, SettingsScreen, ReportsScreen, CheckoutScreen, LoginScreen, PdvScreen) passam.
- [x] F2. Smoke completo via `npm run dev` (Electron): percorrer cada rota (`/login`, `/pdv`, `/pdv/builder/acai`, `/pdv/builder/quick`, `/pdv/new-table`, `/checkout`, `/settings`, `/reports`, `/change-password`) e confirmar: (a) render sem crash, (b) dados carregam, (c) lista das 8 ações-fim-a-fim funciona: login → criar mesa → adicionar item via builder → aplicar promoção → checkout → abrir settings e mudar config de impressora → abrir reports → logout. Screenshot final `/.omo/evidence/final-smoke.png` + checklist preenchido em `/.omo/evidence/final-smoke-checklist.md`.
- [~] F3. Confirmar não-regressão do iFood: com credenciais iFood mockadas (ou setadas via UI), iniciar polling, simular `ifood:new-order` (via botão de teste ou IPC manual) — a mesa delivery aparece na sidebar e o beep toca; simular `ifood:order-cancelled` — toast de erro aparece. Evidência: `/.omo/evidence/final-ifood.txt` (logs).
- [x] F4. Confirmar que `src/App.jsx` foi removido ou reduzido a stub de 1 linha; `grep -rn "App" src/main.jsx src/router/` mostra que main.jsx importa apenas do router; `grep -rln "modals" src/` retorna vazio (o objeto `modals` foi extinto). Evidência: `/.omo/evidence/final-no-modals.txt`.
- [x] F5. Commit único por fase (8 commits) com mensagens `refactor(router): phase N — <summario>`; nenhum commit intermedieiro quebra `npm run dev`. Confirmar via `git log --oneline -8` anexado em `/.omo/evidence/final-commits.txt`.

## Dependency matrix
```
1 (auth store) ──┬─▶ 2 (router) ──▶ 3 (AppLayout) ──▶ 4 (routes map)
                 │                                        │
                 └─▶ 7 (auth screens) ◀───────────────────┤
                                                          ▼
                                 5 (settings) ─▶ 6 (reports+checkout) ─▶ 8 (pdv + demolish) ─▶ F1..F5
```
- 1 não tem dependência anterior.
- 2 depende de 1 (router lê `useAuthStore`).
- 3 depende de 2 (AppLayout é o Component da rota `/`).
- 4 depende de 3 (extende o router com o mapa completo).
- 5,6 dependem de 4 (precisam do slot de rota).
- 7 depende de 1 (usa `useAuthStore`) e de 4 (slot `/login`,`/change-password`); é independente de 5/6.
- 8 depende de 5,6,7 (todas as outras features extraídas; PdvScreen consome `useStore` e é point final que desacopla App.jsx).
- F1-F5 dependem de todos (1-8).
